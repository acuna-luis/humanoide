[Service]
# This Cruzr S2 currently has the factory clamp plates fitted.  Keep the
# controller default aligned with the real end effector even if the robot UI
# reports the generic "gripper" label.
Environment=arm=clamp


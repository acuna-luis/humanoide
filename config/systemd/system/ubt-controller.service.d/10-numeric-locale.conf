[Service]
# XRoboToolkit serializes poses with commas between fields.  Keep decimal
# numbers on a dot regardless of the Ubuntu desktop locale, otherwise the
# controller cannot parse poses deterministically.
Environment=LANG=C.UTF-8
Environment=LC_ALL=C.UTF-8
Environment=LC_NUMERIC=C
